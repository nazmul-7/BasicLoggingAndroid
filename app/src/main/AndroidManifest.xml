<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rahima.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".MainActivity_2" />
        <activity android:name=".AuthenticationHelper.LoginActivity" />
        <activity android:name=".AuthenticationHelper.SignUpActivity" />
        <activity android:name=".AuthenticationHelper.LogoutActivity" />
        <activity android:name=".MainActivity_3" />
        <activity android:name=".FeedbackActivity" />
        <activity android:name=".AboutUsActivity"></activity>
    </application>

</manifest>